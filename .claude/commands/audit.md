Your goal is to udpate any vulnerable dependencies.

Do the following:
1. Run `npm audit` to find vulernable installed packagesin this projec
2. Run `npm audit fix` to apply updates
3. Run tests and verify the updates didn't break anything

